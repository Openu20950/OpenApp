<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- A 'parent' preference, which enables/disables child preferences (below)
         when checked/unchecked. -->

        <SwitchPreference
            android:defaultValue="true"
            android:key="notifications_place_around"
            android:title="@string/pref_title_place_around_notifications" />

        <ListPreference
            android:defaultValue="100"
            android:key="place_around_radius"
            android:title="@string/pref_title_my_radius"
            android:summary="List of radius"
            android:entries="@array/place_around_radius_text"
            android:entryValues="@array/place_around_radius_string"
            />

        <SwitchPreference

            android:dependency="notifications_place_around"
            android:key="friend_only"
            android:title="@string/pref_title_friends_only"
            android:defaultValue="false"/>


    <!-- Allows the user to choose a ringtone in the 'notification' category. -->
    <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
    <!-- NOTE: RingtonePreference's summary should be set to its value by the activity code. -->
    <RingtonePreference
        android:defaultValue="content://settings/system/notification_sound"
        android:dependency="notifications_place_around"
        android:key="notifications_new_message_ringtone"
        android:ringtoneType="notification"
        android:title="@string/pref_title_ringtone" />

    <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
    <SwitchPreference
        android:dependency="notifications_place_around"
        android:key="notifications_new_message_vibrate"
        android:title="@string/pref_title_vibrate"
        android:defaultValue="true"/>

</PreferenceScreen>
